import { NotEmptyDeckPipe } from './not-empty-deck.pipe';

describe('NotEmptyDeckPipe', () => {
  it('create an instance', () => {
    const pipe = new NotEmptyDeckPipe();
    expect(pipe).toBeTruthy();
  });
});
